/* eslint-disable */
import { url } from '@/assets/util.js'

export function CriarCurso(usuario) {
    console.log(usuario);
}

export function EditarCurso(usuario) {
    console.log(usuario);
}

export function DeletarCurso(usuario) {
    console.log(usuario);
}